public class CloseStrings {

    /**
     * Operation 1: swap two characters to match the other string.
     * Operation 2: replace all occurrences of a char with another char.
     */
    public boolean closeStrings(String word1, String word2) {
        int len = word1.length();
//        if (len == word2.length()) {
//
//        }
        return false;
    }

    public static void main(String[] args) {
        String word1 = "baccbb", word2 = "abbbcc";
        word1.inde
    }
}
